# Offline-bundle artifacts (build outputs; never needed inside images)
*.tar.gz
# VCS, local scratch, caches
.git/
.claire/
.claude/
.remember/
.superpowers/
.mypy_cache/
*.code-workspace

# Dev-only eval harness — never part of any image
eval/
data/
