name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    # Pinned to nos-tromo/.github@1dc9e8b until v1 is tagged.
    uses: nos-tromo/.github/.github/workflows/infra-validation.yml@1dc9e8b32be34853c1d967786a15da54ec76322a
    with:
      compose-files: '-f docker/compose.yaml -f docker/compose.override.yaml'
      compose-profiles: '--profile media'
      dockerfiles-glob: 'docker/Dockerfile.*'
      shell-scripts-glob: 'scripts/*.sh'
